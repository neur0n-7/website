export const metadata = {
  title: "Another placeholder post",
  date: "2025-11-02",
  excerpt:
    "A short summary of what this post is about, enough to make someone want to click in and read the rest.",
};

This is placeholder body copy for the post. Replace it with real writing once you've got something to say.

Each post lives in its own `.mdx` file in `src/content/blog/`, so formatting, images, and code blocks are all just regular Markdown/MDX — no extra setup needed per post.
