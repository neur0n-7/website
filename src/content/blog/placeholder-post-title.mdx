export const metadata = {
  title: "Placeholder post title",
  date: "2026-01-14",
  excerpt:
    "A short summary of what this post is about, enough to make someone want to click in and read the rest.",
};

This is placeholder body copy for the post. Replace it with real writing once you've got something to say. You can use **bold**, _italics_, links like [this one](/projects), and anything else regular Markdown supports.

## A heading

Here's an example of a code block:

```ts
export function greet(name: string) {
  return `Hello, ${name}!`;
}
```

And a list:

- One
- Two
- Three

> A blockquote, for pulling out a quote or aside.

Drop an image into `public/` and reference it like `![alt text](/your-image.png)` to have it show up styled automatically.
